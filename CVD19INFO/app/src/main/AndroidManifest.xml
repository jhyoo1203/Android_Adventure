<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.gwnu.mobile.cvd19info">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_appicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MypageActivity"/>
        <activity android:name=".ui.SymptomInfoActivity"/>
        <activity android:name=".ui.SymptomChartActivity"/>
        <activity android:name=".ui.AfterEffectInfoActivity"/>
        <activity android:name=".ui.AfterEffectChartActivity"/>
        <activity android:name=".ui.MainActivity"/>
    </application>

</manifest>